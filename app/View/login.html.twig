{% extends 'base.html.twig' %}

{% block title %} – Login{% endblock %}
{% block head %}
    {{ parent() }}
{% endblock %}
{% block content %}
    <h1>Login</h1>
    <p>Log into your account using the form below.</p>
    <p>Welcome {{ name }}</p>
    <form>
        <input type="text" name="email" autofocus="1" aria-label="Email">
        <input type="password" name="password" aria-label="password">
        <input type="hidden" autocomplete="off" name="remember" value={{ remember }}>
        <input type="hidden" autocomplete="off" name="csrf" value={{ csrf }}>
        <button type="submit" name="login">Log In</button>
    </form>
{% endblock %}